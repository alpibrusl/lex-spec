[package]
name        = "lex-spec"
version     = "0.1.0"
description = "Capability-precondition + spec DSL for Lex — Spec ADT, evaluator, randomized property check, SMT-LIB export."

[dependencies]
lex-schema = { git = "https://github.com/alpibrusl/lex-schema" }
lex-trail = { git = "https://github.com/alpibrusl/lex-trail" }
